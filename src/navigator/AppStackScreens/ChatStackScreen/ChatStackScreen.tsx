import { View, Text } from 'react-native'
import React from 'react'

const ChatStackScreen = () => {
  return (
    <View>
      <Text>ChatStackScreen</Text>
    </View>
  )
}

export default ChatStackScreen